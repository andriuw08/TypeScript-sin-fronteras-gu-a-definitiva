// El nombre del archivo debe ser igula al de la clase
// El export hace que la clase se pueda usar en otro archivo

export class Point {
    constructor( 
        public x: number,
        public y: number,
    ) {}
}
